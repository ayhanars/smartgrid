/** Default document/artboard size, in mm. Shared by the 2D canvas and the
 * 3D viewport so their ground planes always agree on where (0,0) is. */
export const ARTBOARD_WIDTH = 256
export const ARTBOARD_HEIGHT = 256
